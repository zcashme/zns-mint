# TX-005 Harness Record

```text
Slice:
Registry V6 exact-transaction sighash boundary.

Invariant IDs:
TX-005; preserves NAME-001 and TX-006.

Root owner:
Root Codex agent.

Writer and exact file lease:
Root only: src/registry/signing.rs, src/registry/signing.changelog.md,
docs/design/09-transaction-assembly.md, docs/harness/coverage.csv,
docs/harness/tx-005.md.

Initial dirty-worktree snapshot:
Modified: AGENTS.md, docs/design/08-chain-sync.md,
docs/design/09-transaction-assembly.md, docs/protocol.md.
Untracked: .agents/, docs/harness/.

Authority read:
AGENTS.md; docs/harness/invariants.md; docs/design/09-transaction-assembly.md.

Matching changelog and blob hash:
No adjacent changelog existed at the initial snapshot. The first harness
forward test rejected `src/main.changelog.md` as a substitute because that file
explicitly tracks only `src/main.rs`. The lease was corrected by establishing
and reading `src/registry/signing.changelog.md`; repository baseline 35dad3d.

Upstream crate and pinned revision:
zcash_primitives at a97a3d5f46d096b94ceb71271c7d38f20af4e1f1;
unsafe-zns Orchard fork at 34699d38695ad28c37a022155df5420760a29741;
sapling-crypto 0.7.0.

Exact upstream APIs (path, lines, signature or trait bound):
zcash_primitives/src/transaction/mod.rs:243-271 Authorization and Unauthorized;
zcash_primitives/src/transaction/mod.rs:383-419 TransactionData::from_parts_v6;
zcash_primitives/src/transaction/mod.rs:1088-1106 TxDigests and TransparentDigests;
zcash_primitives/src/transaction/sighash.rs:33-55 signature_hash generic bounds;
zcash_primitives/src/transaction/sighash_v6.rs:10-37 V6 digest inputs;
zcash_primitives/src/transaction/txid.rs:292-384 TxIdDigester;
zcash_primitives/src/transaction/builder.rs:1218-1346 complete transaction then signatures;
zns-orchard/src/builder.rs:1713-1730 Bundle::apply_signatures;
sapling-crypto-0.7.0/src/builder.rs:1196-1213 Bundle::apply_signatures;
/Users/jules/ZcashNames/librustzcash/zcash_transparent/src/bundle.rs:56-59 Authorized does not implement TransparentAuthorizingContext.

Observed repository facts:
The unauthorized transaction omitted Sapling while the final transaction
accepted and inserted an already-authorized Sapling bundle. No caller uses the
signer today.

Inferences:
The Ironwood signatures could not authorize the serialized mixed-pool
transaction because its V6 Sapling effecting digest differed.

Contradictions:
The old signing.rs comment claimed omitting Sapling was correct; pinned upstream
V6 sighash code explicitly commits the Sapling digest.

Unresolved questions:
The complete Treasury mixed-pool assembly and fee policy remain separate work.

Design:
Remove the Sapling parameter from the Registry signer, keep Sapling absent in
both phases, and compare the upstream-generated V6 effecting-data digests
(`TxDigests`) of the final authorized transaction against the digests used for
the shielded sighash. Do not call `signature_hash` on
`TransactionData<Authorized>`: its transparent authorization type does not
implement `TransparentAuthorizingContext`.

Rejected alternatives:
Accepting an already-authorized Sapling bundle cannot prove that its binding
signature used this transaction's sighash. Implementing the full mixed-pool
Treasury signer would expand into an unresolved independent slice.

Assumptions:
The current Registry signer remains Ironwood-only. Transparent outputs remain
in scope and are present identically in both transaction phases.

Failure modes:
Future effecting-data mutation; incorrect independent sighash implementation;
unverified mixed-pool fee calculation.

Files changed:
Recorded in the writer lease above.

Tests written:
No standalone test. A production pre-serialization effecting-digest equality
postcondition was added. Mutation and independent-sighash tests remain
required.

Commands actually run and exit status:
No Cargo, formatter, or test command run; prohibited absent explicit user
authorization.

Independent review findings:
Post-write review found that the first postcondition used `signature_hash` on
`TransactionData<Authorized>`, which violates the generic bound requiring
`TransparentAuthorizingContext`. The postcondition was replaced by a direct
comparison of the canonical `TxDigests` effecting components.

Coverage rows updated:
TX-005: contradicted -> implemented. It is not marked tested or reviewed.

Remaining evidence:
Mutation matrix, independent sighash comparison, and executed verification.

Uncommitted changes:
Yes.
```
